{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "release": "./node_modules/.bin/lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.13.2"
  }
}
