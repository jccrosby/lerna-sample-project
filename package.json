{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "registry": "verdaccio",
    "release": "./node_modules/.bin/lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.13.2"
  },
  "publishConfig": {
    "registry": "http://localhost:4873/"
  }
}
